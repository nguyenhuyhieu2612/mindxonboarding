apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: mindx-app
type: Opaque
stringData:
  # WARNING: Change these values in production!
  # Base64 encode sensitive values
  jwt-secret: "mindx-secret-key-change-in-production-use-openssl-rand"
  # Add more secrets as needed
  # database-url: "your-database-connection-string"
  # api-key: "your-api-key"
